# 2026-08-16 工作日志

## 安全与 Bug 审查（drinkexpensetracker 微信小程序）
完成全量代码审查，输出报告：`SECURITY_REVIEW_2026-08-16.md`

关键结论：
- 🔴 C1 严重：客户端（pages/index/index.js:639/662/834 等）直接 `wx.cloud.database()` 写 transactions/settings，绕过云函数权限校验。getTransactions 的 update/delete 权限检查是死代码。任何登录用户可越权改/删他人记录、伪造 creatorOpenId/amount、读全量数据。
- 🔴 C2：因 C1 能跑，集合写规则必然开放（auth!=null）。需收归云函数 + 关客户端写。
- 🔴 C3：缺 owner 引导（initOwner 已删，login 默认 employee，manageUsers 只认 owner）→ 无任何途径产生首店长，店长功能全线不可用。
- 🟠 B1：`getUserProfile` 被 wxml:40 与 index.js:322 调用但未定义 → 登录流程崩溃。
- 🟠 B2：Excel XML 导出未转义 date/category/supplier（仅 remark 部分转义）→ 含特殊字符分类会生成损坏文件（excel.js:47-49、exportExcel:97-100）。
- 🟠 B3：sendReminder 用云函数本地时区算今日，与北京时间差 8h。
- 🟠 B4：settings 集合读写不按 _openid，多用户互相覆盖；与 sendReminder 查 _openid 口径不一致。
- 中危：角色实际只有 owner/非owner 两级（admin/reviewer 未实现）；setPermissions 死代码；金额无服务端校验；getUserInfo 废弃拿匿名资料；模板ID硬编码。
- 注意：项目自带 REVIEW.md 与真实代码严重不符（模板/幻觉内容），不可信。

建议优先级 P0(C1/C2/C3)→P1(B1-B4)→P2/P3。已给出修复示范代码（统一 escapeXml、create 云函数取 WXContext、owner 引导）。用户尚未要求实际改码。
